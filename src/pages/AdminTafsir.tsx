import React from "react";

// Admin tafsir UI removed — this page is a placeholder to avoid broken routes.
export default function AdminTafsir() {
  return (
    <div className="p-6 max-w-3xl mx-auto">
      <h2 className="text-lg font-semibold">Admin — Tafsir Removed</h2>
      <p className="mt-2 text-sm text-muted-foreground">Tafsir import tools have been removed from this build. If you need to re-enable, restore from project history.</p>
    </div>
  );
}
